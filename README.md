Small script that takes in stdin and colors exim/postfix ids (best effort). This
should make it easier to keep track of emails over multiple lines.
Currently it only uses six colors after that they repeat.
